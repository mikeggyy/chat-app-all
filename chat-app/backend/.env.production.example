# 生產環境配置範例
# 複製此文件為 .env.production 並填入實際值
# 注意：Cloud Run 部署時建議使用 Secret Manager 而非 .env 文件

# ==================== 伺服器配置 ====================
NODE_ENV=production
PORT=8080  # Cloud Run 預設使用 8080

# ==================== CORS 配置 ====================
# 允許的前端域名（多個用逗號分隔）
CORS_ORIGIN=https://your-project-id.web.app,https://your-project-id.firebaseapp.com

# ==================== Firebase Admin SDK ====================
FIREBASE_ADMIN_PROJECT_ID=your-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"

# Firebase Emulator（生產環境應為 false）
USE_FIREBASE_EMULATOR=false

# ==================== AI Services ====================
# OpenAI API（用於 GPT-4 對話和 TTS 語音）
OPENAI_API_KEY=sk-...

# Replicate API（用於圖片生成）
REPLICATE_API_TOKEN=r8_...

# Google Generative AI（可選，作為 Replicate 的備選）
GOOGLE_GENERATIVE_AI_API_KEY=...

# ==================== 安全性建議 ====================
# 🔒 請使用 Google Secret Manager 管理敏感資訊
#
# 創建 Secret：
# echo -n "sk-your-openai-key" | gcloud secrets create openai-api-key --data-file=-
#
# 授權 Cloud Run 訪問：
# gcloud secrets add-iam-policy-binding openai-api-key \
#   --member="serviceAccount:PROJECT_NUMBER-compute@developer.gserviceaccount.com" \
#   --role="roles/secretmanager.secretAccessor"
#
# 在 Cloud Run 控制台綁定 Secret 到環境變數
